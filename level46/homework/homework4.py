# 4) Generate a list of the first letters of each word in a given list of words using list comprehension.

words = ["phone", "house", "sport", "car"]
first_letters = [word[0] for word in words]
print(first_letters)